# Python dependencies for automatic spindle / slow-wave detection
#   1_eventDetection/spindlePilot_automaticSpindleSlowwavesYASA.py
# Install with:  pip install -r requirements.txt
#
# Only direct dependencies are listed; YASA pulls in its own runtime
# dependencies (e.g. numba, scikit-learn) automatically.

numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.5.0
mne>=1.0.0
yasa>=0.6.0
